[project]
name = "fptv"
version = "0.1.0"
description = "Fisher Price TV for Raspberry Pi and tvheadend"
requires-python = ">=3.9"
dependencies = [
  "urwid>=2.6",
  "gpiozero>=2.0",
  # If you parse m3u and fetch from tvheadend:
  "requests>=2.31",
]

[project.scripts]
toytv = "toytv.app:main"
